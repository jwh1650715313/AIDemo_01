// LocalSecrets.xcconfig is intentionally ignored by git.
// Put machine-local API keys there for local Release builds.
#include? "LocalSecrets.xcconfig"

ARK_MODEL = doubao-seed-character-251128
ARK_CHAT_COMPLETIONS_URL = https:/$()/ark.cn-beijing.volces.com/api/v3/chat/completions
